% !TEX program = xelatex
% !TEX root = ../../Text/main.tex
% !TEX encoding = UTF-8

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% The Legrand Orange Book
% LaTeX Template
% Version 2.3 (8/8/17)
%
% This template has been downloaded from:
% http://www.LaTeXTemplates.com
%
% Original author:
% Mathias Legrand (legrand.mathias@gmail.com) with modifications by:
% Vel (vel@latextemplates.com)
%
% License:
% CC BY-NC-SA 3.0 (http://creativecommons.org/licenses/by-nc-sa/3.0/)
%
% Compiling this template:
% This template uses biber for its bibliography and makeindex for its index.
% When you first open the template, compile it from the command line with the 
% commands below to make sure your LaTeX distribution is configured correctly:
%
% 1) pdflatex main
% 2) makeindex main.idx -s StyleInd.ist
% 3) biber main
% 4) pdflatex main x 2
%
% After this, when you wish to update the bibliography/index use the appropriate
% command above and make sure to compile with pdflatex several times 
% afterwards to propagate your changes to the document.
%
% This template also uses a number of packages which may need to be
% updated to the newest versions for the template to compile. It is strongly
% recommended you update your LaTeX distribution if you have any
% compilation errors.
%
% Important note:
% Chapter heading images should have a 2:1 width:height ratio,
% e.g. 920px width and 460px height.
%
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%


\documentclass[12pt]{book} 

\newcommand\myStructure{../../Style/Structure}
\usepackage{\myStructure/config}
\usepackage{\myStructure/list}
\usepackage{\myStructure/theorem}
\usepackage{\myStructure/remark}
\usepackage{\myStructure/bib}
\usepackage{\myStructure/index}
\usepackage{\myStructure/myexercise}

\newcommand\myFormat{../../Style/Format}
\usepackage{\myFormat/color}
\usepackage{\myFormat/font}
\usepackage{\myFormat/framed}
\usepackage{\myFormat/toc}
\usepackage{\myFormat/toc-part-number}
\usepackage{\myFormat/header}
\usepackage{\myFormat/section-title}
\usepackage{\myFormat/section-margin-number}
\usepackage{\myFormat/section-part-image}
\usepackage{\myFormat/section-chapter-image}
\usepackage{\myFormat/section-chapter}
\usepackage{\myFormat/hyper}

\usepackage{../../Style/Structure/last}% DO NOT COMMENT OUT

\providecommand\mynot{\ensuremath{\mathop{\textbf{\textsf{non}}}}}
\providecommand\mytrue{\ensuremath{\mathord{\textbf{\textsf{vrai}}}}}
\providecommand\myfalse{\ensuremath{\mathord{\textbf{\textsf{faux}}}}}
\providecommand\myor{\ensuremath{\mathbin{\textbf{\textsf{ou}}}}}
\providecommand\myxor{\ensuremath{\mathbin{\textbf{\textsf{ou bien}}}}}
\providecommand\myand{\ensuremath{\mathbin{\textbf{\textsf{et}}}}}
\providecommand\myorand{\myover{\myor}{\myand}}
\providecommand\myandor{\myover{\myand}{\myor}}
\providecommand\myover[2]{\mathbin{\genfrac{}{}{0pt}{1}{\small#1}{\small#2}}}
\providecommand\mybydef[1]{\overset{\text{\scriptsize déf}}{#1}}
\providecommand{\mynewcolumntype}[2]{\newcolumntype{#1}{>{\(}wc{#2}<{\)}|}}
\graphicspath{{../Pictures/}{../../../ODM/Chapitres/}}

\makeatletter
\newcommand\arraybslash{\let\\\@arraycr} % LibreOffice cs name
\makeatother
